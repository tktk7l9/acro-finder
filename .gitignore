# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# typescript
*.tsbuildinfo
next-env.d.ts

# misc
.DS_Store
*.log

# local env files
# keyway pull -e <env> は .env.development / .env.production という名前で書き出す。
# .env*.local と .env だけだとそれらが素通りして、public リポジトリに Resend の
# API キーが載る。.env.* をまとめて無視し、example だけ通す。
.env
.env.*
!.env*.example

# vercel
.vercel

# coverage
/coverage

# @opennextjs/cloudflare のビルド生成物 / wrangler のローカル状態
.open-next/
.wrangler/
